import 'package:flutter/material.dart';

// Dark Theme Colors
const backgroundColor = Color(0xFF0B0F1A);
const secondaryBackground = Color(0xFF0F172A);
const cardColor = Color(0xFF111827);

// Primary Colors (Gradient Neon)
const primaryColor = Color(0xFF6C63FF);
const secondaryColor = Color(0xFF4F46E5);
const accentColor = Color(0xFF3B82F6);

// Text Colors
const textPrimary = Color(0xFFFFFFFF);
const textSecondary = Color(0xFF94A3B8);
const textMuted = Color(0xFF64748B);

// Glow Effect
const glowColor = Color(0xFF6C63FF);

// Gradients
const primaryGradient = LinearGradient(
  colors: [
    Color(0xFF6C63FF),
    Color(0xFF3B82F6),
  ],
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
);

const cardGradient = LinearGradient(
  colors: [
    Color(0xFF1E293B),
    Color(0xFF0F172A),
  ],
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
);

// Border Gradient
const borderGradient = LinearGradient(
  colors: [
    Color(0xFF6C63FF),
    Color(0xFF3B82F6),
    Color(0xFF6C63FF),
  ],
);
